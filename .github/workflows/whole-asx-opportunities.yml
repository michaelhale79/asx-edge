name: ASX Edge Whole ASX Opportunities

on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/whole_asx_opportunities.py'
      - 'scripts/whole_asx_runner.py'
      - '.github/workflows/whole-asx-opportunities.yml'
  schedule:
    - cron: "35 7 * * 1-5"

permissions:
  contents: write

concurrency:
  group: asx-edge-data
  cancel-in-progress: false

jobs:
  whole-asx-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install ASX universe reader
        run: python -m pip install --disable-pip-version-check xlrd

      - name: Scan entire ASX for opportunities
        run: python scripts/whole_asx_runner.py

      - name: Commit whole-ASX discoveries and study snapshot
        run: |
          git config user.name "ASX Edge Bot"
          git config user.email "asx-edge@users.noreply.github.com"
          git add data.json
          if [ -d study/market_snapshots ]; then git add study/market_snapshots; fi
          git diff --cached --quiet || git commit -m "Update whole ASX opportunities"
          git pull --rebase origin main
          git push
